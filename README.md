CRITICAL OUTPUT CONTRACT:

- Output ONLY a valid TigerGraph GSQL INTERPRET QUERY block.
- The output MUST start with the keyword INTERPRET.
- All SELECT statements MUST be inside the INTERPRET QUERY block.
- Do NOT output a standalone SELECT query.
- Do NOT include explanations, markdown, or comments.
- Do NOT include triple backticks.
