Every statement that performs an ACCUM update must be terminated by a semicolon (;). The semicolon must come after the update statement and before the start of the next line, statement, or traversal.
